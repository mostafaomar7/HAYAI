# الـ Plans كنظام تحكّم في الـ Features — مطلوب من الباك اند

> **المطلوب:** الأدمن يقدر يفتح ويقفل كل feature جوّه كل نوع أكونت من لوحة التحكم.

## المطلب الأصلي (من صاحب السيستم)

> «الـ plans كويسة أوي إنها بتقدر تعمل بيها زي الـ system setting، بس محتاجة يبقى في الـ plan يقدر يتشال أو يتضاف فيها كل الـ features اللي جوّه كل أكونت. يعني مثلًا **مافيش طريقة إني أسيب الـ jobs بس اللي تظهر في كل plan والباقي كله يتقفل**. وتكون بتتحدّث — لما تبقى feature جديدة تظهر كجديدة. وأعرف ألغي الـ plan القديمة أو أعدّلها. والمهم يبقى فيها كل الـ features.»

---

## 00 — اللي موجود دلوقتي

الجدول ده متحقّق منه بالفعل عن طريق probe على السيرفر. كود `401` معناه الراوت موجود ومحتاج توثيق، و`405` معناه الـ verb ده مش مسجّل.

| Method | Endpoint | الحالة |
|---|---|---|
| `GET` · `POST` | `/admin/subscription/plans` | ✅ موجود |
| `GET` · `PUT` · `DELETE` | `/admin/subscription/plans/{id}` | ✅ موجود |
| `GET` | `/admin/subscription/plans/modules?plan_type=` | ✅ موجود |
| `POST` · `PUT` · `DELETE` | `/admin/subscription/plans/modules` | ❌ read-only |

يعني **«ألغي أو أعدّل الـ plan القديمة» موجود فعلًا** — `PUT` و `DELETE` مسجّلين. اللي ناقص حاجة تانية.

شكل الـ catalog الحالي، وشكل الـ modules جوّه الـ plan:

```jsonc
// GET /admin/subscription/plans/modules?plan_type=hospital
[ { "plan_type": "hospital", "key": "...", "name": "...", "description": "..." } ]

// plan.modules
[ { "id": 1, "module_key": "...", "module_name": "...",
    "included": true, "description": null, "limit": null } ]
```

**قيم `plan_type` المدعومة:** `hospital`، `clinic`، `doctor`، `general_practitioner`، `specialist`، `consultant`، `pharmacies`، `labs_radiology`، `medical_insurance`، `home_care`، `physical_therapy`، `employment_office`، `medical_devices`.

---

## 01 — المشكلة

الـ catalog **ناقص**. لوحة التحكم بتعرض بالظبط اللي `/plans/modules` بيرجّعه — فلو الـ feature مالهاش `key`، الأدمن مش قادر يقفلها من الأساس. عشان كده مفيش طريقة نقول «Jobs بس».

وكمان مفيش أي إشارة لما feature جديدة تنزل: الـ plans القديمة مش بتعرف إن في حاجة جديدة ظهرت، والأدمن مش هيلاحظ إلا لو قعد يقارن بإيده.

---

## 02 — استكمال الـ catalog

القاعدة: **لو في شاشة أو أكشن في التطبيق، لازم يكون ليها `key` في الـ catalog.** اتحاد مفاتيح أي `plan_type` لازم يساوي كل القدرات اللي الأكونت ده بيستخدمها.

> **محتاجين منكم:** الليست الكاملة للـ keys لكل `plan_type` — عشان نظبط الترجمة والتجميع في الداشبورد. دي أهم مخرَج في الورقة دي.

---

## 03 — الـ catalog يعرف الجديد

حقول إضافية مطلوبة على الـ catalog row:

| الحقل | الغرض |
|---|---|
| `group` | تصنيف — الليست هتبقى طويلة والداشبورد محتاج يقسّمها لأقسام |
| `created_at` | عشان الداشبورد يحطّ badge «جديد» على أي feature اتضافت بعد آخر حفظ للـ plan |
| `default_included` | سلوك محدَّد للـ feature الجديدة على الـ plans الموجودة |
| `sort_order` | ترتيب ثابت بدل ما يتغيّر كل request |
| `supports_limit` | هل المفتاح ده بيقبل رقم ولا on/off بس |

### ⚠️ قرار محتاج تأكيد صريح

لما feature جديدة تتضاف، الـ plans القديمة تعمل إيه؟

- **مقفولة افتراضيًا** — الأدمن هو اللي يفتحها. **دي اللي بنرشّحها**: مفيش مشترك بياخد حاجة من غير قرار.
- **مفتوحة افتراضيًا** — كل المشتركين ياخدوها فورًا.

المهم إنه يبقى قرار مكتوب، مش نتيجة جانبية للـ implementation.

---

## 04 — التطبيق الفعلي للقفل (Enforcement)

> ### 🔴 أهم نقطة في الورقة
> تخزين `included: false` لوحده ملوش أي قيمة لو الـ API مش بيمنع فعلًا. الداشبورد دلوقتي بيكتب الفلاجز وبس — من غير النقطة دي الشاشة بتبقى شكل من غير مفعول.

1. middleware أو gate بيقرا modules الـ plan النشطة للمشترك.
2. أي endpoint تابع لـ module مقفول يرجّع **403** بكود مقروء آليًا، مش رسالة نص.
3. الأكونت يقدر يقرا الـ features بتاعته عشان الموبايل **يخفي الواجهة** بدل ما يفشل بعد الضغط.

```jsonc
// 403 على feature مقفولة
{ "success": false, "code": "FEATURE_NOT_IN_PLAN",
  "message": "...", "data": { "module_key": "jobs" } }

// GET /me/features  — أو تتحط جوّه الـ profile payload
{ "jobs": true, "appointments": false }
```

---

## 05 — الـ Limits مش بس on/off

الحقل `limit` موجود في الـ response بتاع الـ modules بس مش مستخدم في أي حتة. محتاجين تأكيد: هل مدعوم فعلًا؟ (يعني «10 إعلانات وظايف في الشهر»). لو أيوة:

- أنهي keys بتقبل limit وأنهي لأ؟ (الحقل `supports_limit` فوق)
- الوحدة إيه ومدة التصفير كام — شهري / سنوي / مدى الاشتراك؟
- لما المشترك يوصل للحد، الـ endpoint بيرجّع إيه؟ نرشّح **429**، أو 403 بكود مختلف عن `FEATURE_NOT_IN_PLAN` عشان الموبايل يفرّق بين «مش في باقتك» و«خلصت».

لو مش مدعوم، قولوا وهنشيله من الواجهة بدل ما يفضل حقل ميت.

---

## 06 — تعديل ومسح الـ Plans

الـ `PUT` و `DELETE` موجودين، بس السلوك محتاج يتحدّد:

| الحالة | السؤال |
|---|---|
| مسح plan ليها مشتركين نشطين | ترفض برسالة واضحة؟ ولا soft-delete والاشتراكات الحالية تكمل لحد ما تخلص؟ |
| تعديل modules لـ plan شغّالة | يأثّر على المشتركين الحاليين فورًا؟ ولا على الاشتراكات الجديدة بس؟ |
| تغيير `plan_type` لـ plan موجودة | نرشّح **ترفض** — بتخلّي modules الـ plan مالهاش معنى |

النقطة التانية **قرار منتج مش تقني** — لازم ينزل مكتوب، لأن الأدمن محتاج يعرف نتيجة ضغطة Save قبل ما يضغطها.

> **تحقّق أولًا:** كانت في مشكلة سابقة في رسالة الـ delete ومودولات المستشفيات، محتاجة `php artisan migrate --force` و `php artisan config:clear` و `php artisan route:clear` على السيرفر. لو لسه ماتعملتش، دي أول خطوة قبل أي حاجة تانية هنا.

---

## 07 — الشكل المطلوب للـ Response

```jsonc
GET /admin/subscription/plans/modules?plan_type=hospital
{
  "success": true,
  "data": [
    {
      "plan_type": "hospital",
      "key": "jobs",
      "name": "Jobs",
      "description": "نشر وإدارة إعلانات الوظائف",
      "group": "recruitment",
      "sort_order": 10,
      "default_included": false,
      "supports_limit": true,
      "created_at": "2026-08-01T00:00:00Z"
    }
  ]
}
```

الـ plan نفسها تفضل زي ما هي، بشرطين: الـ `limit` يبقى شغّال، وكل مفاتيح الـ catalog تكون موجودة في الأراي — اللي مش متفعّل يرجع `included: false` بدل ما يختفي، عشان نفرّق بين «مقفولة بقرار» و«جديدة لسه متسجّلتش».

---

## 08 — أسئلة محتاجين إجابتها

| # | السؤال |
|---|---|
| 1 | الليست الكاملة للـ keys لكل `plan_type` — **دي البلوكر الأساسي** |
| 2 | الـ feature الجديدة تبقى مقفولة ولا مفتوحة على الـ plans القديمة؟ |
| 3 | الـ enforcement شغّال دلوقتي فعلًا، ولا الفلاجز متخزّنة وبس؟ |
| 4 | الـ `limit` مدعوم؟ |
| 5 | مسح plan ليها مشتركين — بيعمل إيه؟ |
| 6 | تعديل modules — بيأثّر على المشتركين الحاليين ولا لأ؟ |

---

## 09 — اللي هيتعمل في الداشبورد بعد ما ده ينزل

- تقسيم الـ modules لأقسام بالـ `group` مع خانة بحث — الليست هتبقى طويلة
- badge «جديد» على أي feature اتضافت بعد آخر حفظ للـ plan
- زراير «افتح الكل» و«اقفل الكل» لكل قسم
- خانة إدخال للـ `limit` على المفاتيح اللي بتدعمه
- عرض عدد المشتركين على كل plan قبل المسح
- رسالة تأكيد واضحة بنتيجة التعديل على المشتركين الحاليين

---

*الراوتات والـ payloads في الورقة دي متحقّق منها من كود الفرونت ومن probe مباشر على السيرفر. أي حاجة مبنية على افتراض مكتوب جنبها إنها سؤال مش تقرير.*
